// client_type is now a free-form label — no longer constrained to a predefined list.
// Users can enter any value (e.g. "openai", "anthropic", "my-custom-api") for categorization.
// The server treats it as metadata only; it does not affect key instantiation logic.
